---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: go-podcaster-data
  namespace: prod
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: iscsi
