---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: postgres
  namespace: flux-system
spec:
  interval: 30m
  timeout: 15m
  upgrade:
    remediation:
      retries: 3
  chart:
    spec:
      chart: postgresql
      version: "18.x"
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: flux-system
      interval: 12h
  values:
    global:
      security:
        allowInsecureImages: true

    image:
      registry: docker.io
      repository: pgvector/pgvector
      tag: pg18
      pullPolicy: IfNotPresent

    primary:
      podSecurityContext:
        enabled: true
        fsGroup: 999
        runAsUser: 999
        runAsGroup: 999

      containerSecurityContext:
        enabled: true
        runAsUser: 999
        runAsNonRoot: true
        allowPrivilegeEscalation: false
        readOnlyRootFilesystem: false
        seccompProfile:
          type: RuntimeDefault
        capabilities:
          drop:
            - ALL

      extraEnvVars:
        - name: GRAFANA_DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: grafana-db-credentials
              key: grafana-db-password
        - name: OPENWEBUI_DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: openwebui-db-credentials
              key: openwebui-db-password
      initdb:
        scripts:
          init-vector.sql: |
            CREATE EXTENSION IF NOT EXISTS vector;
          init-grafana.sh: |
            #!/bin/bash
            psql -U postgres \
              -v grafana_password="$GRAFANA_DB_PASSWORD" \
              -c "CREATE ROLE IF NOT EXISTS grafana WITH LOGIN PASSWORD :'grafana_password';"
            createdb -U postgres -O grafana grafana || true
          init-openwebui.sh: |
            #!/bin/bash
            psql -U postgres \
              -v openwebui_password="$OPENWEBUI_DB_PASSWORD" \
              -c "CREATE ROLE IF NOT EXISTS openwebui WITH LOGIN PASSWORD :'openwebui_password';"
            createdb -U postgres -O openwebui openwebui || true

      persistence:
        enabled: true
        storageClass: iscsi
        accessModes:
          - ReadWriteOnce
        size: 50Gi

      resources:
        requests:
          cpu: 250m
          memory: 256Mi
        limits:
          cpu: 1000m
          memory: 1Gi

    auth:
      database: postgres
      existingSecret: postgres-credentials
      secretKeys:
        adminPasswordKey: postgres-password

    tls:
      enabled: false

    service:
      type: ClusterIP
      ports:
        postgresql: 5432

    readReplicas:
      replicaCount: 0

    metrics:
      enabled: false

    volumePermissions:
      enabled: true
      securityContext:
        runAsUser: "0"
