machine:
  install:
    disk: /dev/nvme0n1
    extensions:
      - image: ghcr.io/siderolabs/iscsi-tools:v0.1.4
  network:
    interfaces:
      - interface: enp0s31f6
        dhcp: true
        vip:
          ip: 10.7.2.9
